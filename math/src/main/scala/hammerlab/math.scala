package hammerlab

import org.hammerlab.math.{ all, types }

/**
 * Expose various features for importing via:
 *
 * {{{
 * import hammerlab.math._
 * }}}
 */
object math
  extends all
    with types
